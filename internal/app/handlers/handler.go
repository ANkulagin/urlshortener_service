package handlers

import (
	"net/http"
)

// ShortenURL обрабатывает POST-запрос для сокращения URL
func ShortenURL(w http.ResponseWriter, r *http.Request) {
	// Реализуйте обработку POST-запроса и генерацию сокращенного URL
}

// RedirectURL обрабатывает GET-запрос для редиректа на оригинальный URL
func RedirectURL(w http.ResponseWriter, r *http.Request) {
	// Реализуйте обработку GET-запроса и редирект на оригинальный URL
}
